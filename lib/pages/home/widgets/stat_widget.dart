

import 'package:flutter/material.dart';

class HomePageStat extends StatelessWidget {
  const HomePageStat({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
